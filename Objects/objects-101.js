let laptop ={
    name: 'Macbook',
    price: 'Expensive',
    built: 'Alloy'
}

console.log(`My laptop is an ${laptop.name} and it is made of ${laptop.built}`)


let person ={
    name: 'Parv',
    age: 19,
    location: 'Hyderabad'
}

console.log(`${person.name} is ${person.age} and lives in ${person.location}.`)
person.age+=1
console.log(`${person.name} is ${person.age} and lives in ${person.location}.`)
